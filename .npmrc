# Pin the tree exactly. Reproducible installs are a precondition for the measurement
# layer: a transitive version drift between a `before` and an `after` run would show up
# as a performance delta we would then attribute to our own change.
save-exact=true
